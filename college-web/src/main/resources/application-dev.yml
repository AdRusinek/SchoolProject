spring:
  datasource:
    url: jdbc:mysql://localhost:3306/college_dev?&useTimezone=true&serverTimezone=UTC
    username: college_dev_user
    password: rusinek
    platform: mysql
  jpa:
    hibernate:
      ddl-auto: validate
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    database: mysql
    show-sql: true
#    properties:
#      javax:
#        persistence:
#          schema-generation:
#            create-source: metadata
#            scripts:
#              action: create
#              create-target: college_database_create.sql